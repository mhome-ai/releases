name: Release native linux-x64

on:
  push:
    tags:
      - "nlx[0-9]*"
  workflow_dispatch:
    inputs:
      tag:
        description: "Product tag, for example nlx1.2.3"
        required: true
        type: string
      initialize_catalog:
        description: "Initialize a missing platform Stable Catalog"
        type: boolean
        default: false

permissions:
  contents: write
  id-token: write
  packages: write

jobs:
  run:
    uses: ./.github/workflows/run-tagged.yaml
    concurrency:
      group: linux-docker-host
      cancel-in-progress: false
    with:
      runner_labels: '["self-hosted","Linux","AMD64","release-linux-amd64"]'
      channel: native
      tag: ${{ github.event_name == 'workflow_dispatch' && inputs.tag || github.ref_name }}
      initialize_catalog: ${{ github.event_name == 'workflow_dispatch' && inputs.initialize_catalog || false }}
      orchestrator_ref: ${{ github.event_name == 'workflow_dispatch' && github.sha || '' }}
      work_suffix: linux-x64
    secrets: inherit
